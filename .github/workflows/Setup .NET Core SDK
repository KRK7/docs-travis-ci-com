- الاسم: Setup .NET Core SDK
  يستخدم: Actions/setup-dotnet@v3.2.0
  مع:
    # إصدار (إصدارات) SDK اختياري للاستخدام. إذا لم يتم توفيره ، فسيتم تثبيت الإصدار global.json عند توفره. أمثلة: 2.2.104 ، 3.1 ، 3.1.x ، 3.x ، 6.0.2xx
    إصدار دوت نت: # اختياري
    # جودة اختيارية للبناء. القيم المحتملة هي: يوميًا ، موقع ، تم التحقق منه ، معاينة ، ga.
    جودة الشبكة: # اختياري
    # موقع global.json اختياري ، إذا لم يكن موقع global.json موجودًا في جذر الريبو.
    global-json-file: # اختياري
    # مصدر حزمة اختياري لإعداد المصادقة. سوف يستشير أي NuGet.config موجود في جذر الريبو ويقدم NuGet.config مؤقتًا باستخدام متغير البيئة NUGET_AUTH_TOKEN باعتباره ClearTextPassword
    المصدر- url: # اختياري
    # مالك اختياري لاستخدام الحزم من GitHub Package Registry من المؤسسات / المستخدمين بخلاف مالك المستودع الحالي. يُستخدم فقط إذا تم توفير عنوان URL لـ GPR أيضًا في عنوان url المصدر
    المالك: # اختياري
    # موقع NuGet.config الاختياري ، إذا لم يكن NuGet.config الخاص بك موجودًا في جذر الريبو.
    ملف التكوين: # اختياري
    # إدخال اختياري لتمكين التخزين المؤقت لمجلد الحزم العمومية NuGet
    مخبأ: # اختياري
    # يستخدم لتحديد المسار إلى ملف التبعية: bund.lock.json. يدعم أحرف البدل أو قائمة أسماء الملفات للتخزين المؤقت للعديد من التبعيات.
    مسار تبعية التخزين المؤقت: # اختياري
